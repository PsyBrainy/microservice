
server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git: git@github.com:PsyBrainy/microservice.git
        searchPath: config-data
        username: ${GIT_USER}
        password: ${GIT_PASS}
  security:
    user:
      name: root
      password: s3cr3t